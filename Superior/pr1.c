#include<stdio.h>
main(){
    int n,arr[a],sum=0;
    printf("Enter size of arr : ");
    scanf("%d",&a);

    for (int i = 0; i < n; i++)
    {
        printf("a[%d] = ",i);
        scanf("%d",&arr[i]);
    }
    for (int i = 0; i < n; i++)
    {
        sum+=arr[i];
    }
    printf("\n Sum = %d",sum);
    
}